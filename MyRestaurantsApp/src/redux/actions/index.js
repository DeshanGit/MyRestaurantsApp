import * as WeatherActionCreators from './weather_action';
import * as RestaurantsActionCreators from './restaurants_action';

export const ActionCreators = {
    WeatherActionCreators,
    RestaurantsActionCreators,
}